# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Data / artifacts (large; keep out of git — store on the GPU box / HF / GCS)
*.parquet
*.safetensors
*.pt
*.npy
*.npz
activations/
checkpoints/
outputs/
runs/
wandb/
data/          # datasets fetched by scripts/fetch_data.py — external, not committed

# Notebooks / scratch
.ipynb_checkpoints/
scratch/
*.log
