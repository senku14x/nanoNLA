# GPU-box deps (install only on the H200). Pin exact versions once a working point
# is found; versions left as floors here.
-r requirements.txt
torch>=2.4                 # H200 (sm_90) needs a CUDA-12 torch; 2.4+ is fine
transformers>=4.51.0       # CRITICAL: Qwen3 support landed in 4.51 (4.44 will NOT load Qwen3-8B)
safetensors>=0.4
accelerate>=0.33
peft>=0.11                 # LoRA: loading/merging syvb's AV/AR adapters (Gate 0+)
sentencepiece>=0.2         # some tokenizers
httpx>=0.27
orjson>=3.10

# --- semantic text baseline (D7) — install when we build it ---
# sentence-transformers>=3.0    # embedding_auroc tier
# anthropic                     # LLM-judge (read-rate + llm_judge_decodability)

# --- Gate 3 RL inside the nanoNLA fork — use nanoNLA's own requirements ---
# (peft above + datasets, wandb, and bitsandbytes ONLY if you keep 4-bit; we go
#  bf16 so bitsandbytes is optional). `pip install -r requirements.txt` in nanoNLA.
# sglang / vllm are NOT needed for the validated train_rl_self_contained path.

