# GPU-box deps (install only on Vast/Lambda). Pin exact versions once a working
# point is found; sglang>=0.5.6 is required for the input_embeds endpoint used by
# AV serving. vllm is for the Gate-3 RL rollout. Versions left as floors here.
-r requirements.txt
torch>=2.4
transformers>=4.44
safetensors>=0.4
accelerate>=0.33
httpx>=0.27
orjson>=3.10
# AV generation / RL (later gates):
# sglang>=0.5.6
# vllm
